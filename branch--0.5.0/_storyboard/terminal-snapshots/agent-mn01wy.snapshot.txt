SESSION: agent-mn01wy | silver-condor
CANVAS:  storyboarding/form-schema
BRANCH:  0.5.0
TIME:    2026-05-11T09:09:02.279Z

================================================================================
SCREEN
================================================================================

● Signal done with env (shell)
  │ export STORYBOARD_WIDGET_ID=agent-mn01wy
  │ STORYBOARD_CANVAS_ID=storyboarding/form-schema
  │ npx storyboard agent signal --status done --message "Fixed doubled ArtifactForm
  │ chrome inside Dialog"
  └ 3 lines...

● Done. The doubled framing came from ArtifactForm's .form always rendering as a card
  (padding + background + border + border-radius). Inside CreateDialog, that nested
  card sat inside the Dialog body, so you saw two stacked frames.

  Fix: when hideHeader is true (the embedded-in-Dialog case), the <form> now gets a
  data-bare attribute, and the CSS strips padding, background, border, and
  border-radius. Standalone usage of ArtifactForm keeps its card.

   - packages/storyboard/src/internals/ArtifactForm/ArtifactForm.jsx — added
  data-bare={hideHeader || undefined}
   - packages/storyboard/src/internals/ArtifactForm/ArtifactForm.module.css — added
  .form[data-bare] reset

  Summary widget posted next to your terminal on the canvas.

● Shell "Find ArtifactForm usage" completed

● Acknowledged — that was the earlier repo-wide grep, no longer needed.

 ~/workspace/storyboard-core/worktrees/0.5.0 [⎇ 0.5.0*%]               @ArtifactForm.jsx
─────────────────────────────────────────────────────────────────────────────────────────
❯  
─────────────────────────────────────────────────────────────────────────────────────────
 / commands · ? help        terminal-agent · Claude Opus 4.7 (1M context)(Internal only)

================================================================================
SCROLLBACK (last 60s)
================================================================================

 ~/workspace/storyboard-core/worktrees/0.5.0 [⎇ 0.5.0*%]               @ArtifactForm.jsx
