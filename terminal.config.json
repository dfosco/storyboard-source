{
  "$comment": "Project-level overrides for terminal + canvas agents. Leaf-level merge: only keys here are overridden, everything else inherits the library defaults from node_modules/@dfosco/storyboard/terminal.config.json. See .storyboard/scaffold/terminal.config.json for the full default reference.",
  "terminal": {
    "fontSize": 18,
    "fontFamily": "'Ghostty', 'SF Mono', 'Menlo', 'Monaco', 'Courier New', monospace"
  },
  "agents": {
    "copilot": {
      "startupCommand": "copilot --remote --agent terminal-agent",
      "resumeCommand": "copilot --resume={id} --remote --agent terminal-agent"
    }
  }
}
